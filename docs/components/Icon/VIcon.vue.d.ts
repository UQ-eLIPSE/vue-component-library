import Vue, { VueConstructor } from 'vue';
declare const VIcon: VueConstructor<{
    iconType: string;
    iconSize: string;
} & {
    type: string;
    size: string;
    color: string;
} & Vue>;
export default VIcon;
